spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres # Points to local Docker container
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver